% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DEtimeMatern32KernCompute.R
\name{DEtimeMatern32KernCompute}
\alias{DEtimeMatern32KernCompute}
\title{Compute the DEtime Matern 32 kernel given the parameters and X}
\usage{
DEtimeMatern32KernCompute(kern, x, x2)
}
\arguments{
\item{kern}{DEtime Matern 32 kernel structure to be computed}

\item{x}{Depending on the number of inputs, x can be the input data matrix (rows are data points) to the kernel computation, or the first input matrix to the kernel computation (forms the rows of the kernel)}

\item{x2}{Second input matrix to the kernel computation (forms the columns of the kernel)}
}
\value{
DEtime Matern32 kernel structure.
}
\description{
Compute the DEtime Matern32 kernel given the parameters and X.
}
\details{
\code{K <- DEtimeMatern32KernCompute(kern, x)} computes a DEtime Matern32 kernel matrix given an input data matrix.
   \code{K <- DEtimeMatern32KernCompute(kern, x1, x2)} computes a DEtime Matern32 kernel matrix for the given kernel type given two input data matrices, one for the rows and one for the columns.
}
\examples{
kern <- list()
kern <- DEtimeKernParamInit(kern)
K <- DEtimeMatern32KernCompute(kern, as.matrix(3:8))
}

